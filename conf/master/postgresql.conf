    # Allow connections from any IP address (within the Docker network)
    listen_addresses = '*'

    # Enable replication logs (Write-Ahead Log)
    wal_level = replica

    # Number of concurrent slave connections
    max_wal_senders = 10

    # Amount of WAL logs to keep for slaves that might fall behind
    wal_keep_size = 64MB
